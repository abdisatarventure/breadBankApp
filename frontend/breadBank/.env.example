# Copy to frontend/breadBank/.env (or .env.local) and adjust as needed.

# Base URL of the BreadBank API. Defaults to http://localhost:3000/api when unset.
# Set this for any non-local deployment, e.g. https://api.yourdomain.com/api
VITE_API_URL=http://localhost:3000/api
